/**
 *
 * @param {array} array What's your array I'm curious what it is.
 * @param {string} order descending order = false|| ascending order = true
 */

function bubbleSort(array, order) {
  if (order) {
    let newArray = [];
    for (let i = 0; i < array.length; i++) {
      for (let j = 0; j < array.length; j++) {}
    }
  }
}
bubbleSort();

/**
 * @param {string} somebody - Somebody's name.
 */
